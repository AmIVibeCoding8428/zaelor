# Anthropic API key — used to convert structured JSON output into client-facing prose.
# Never commit a real key. Set this in Render's environment variables in production.
ANTHROPIC_API_KEY=

# Comma-separated list of allowed CORS origins (e.g. the Vercel deployment URL).
# Leave blank / "*" for local development.
ALLOWED_ORIGINS=*
